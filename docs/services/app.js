const side_bar = document.getElementById('sidebar');
const menu = document.getElementById('menu-toggle-close');

menu.addEventListener('click', ()=>{
    side_bar.classList.toggle = 'active';
});
